# Detection Notes — LLMNR Poisoning

## Detection Approach

### Network Layer
- IDS (Suricata) rules monitor UDP traffic on port **5355**, which is used by LLMNR.
- Identify multicast traffic directed to **224.0.0.252**.
- Investigate systems responding to LLMNR queries that are not legitimate infrastructure hosts.
- Confirm whether SMB authentication attempts follow LLMNR responses.

### Host Layer (Windows Event Logs + Sysmon)

- **Event ID 4625** – Failed logon attempts generated by NTLM authentication failures.
- **Event ID 4648** – Explicit credential usage when authentication is attempted using provided credentials.
- **Sysmon Event ID 3 (NetworkConnect)** – Network connection events showing victim hosts connecting to attacker-controlled systems on **port 445 (SMB)**.

These artifacts may occur when a victim machine attempts NTLM authentication to a rogue responder.

---

## Corelation Strategy 

The goal of this detection is to identify systems attempting LLMNR name resolution followed by NTLM authentication to the responding host. 

Successful detection requires correlation across network and host telemetry.

Key behavioral pattern:

→ LLMNR request (UDP 5355)
→ Rogue responder answers query (LLMNR response)
→ Victim attempts SMB authentication to responder (port 445)

Typical timeline:

LLMNR query → NTLM auth attempt within seconds or to the responding host within 1–30 seconds

If an SMB session is successfully established with the responding host, escalate severity.

---

## False Positives & Tuning
- Some IoT devices or printers use multicast name resolution legitimately.  
- Maintain a whitelist known infrastructure devices that legitimately use multicast name resolution i.e. Domain Controllers, Utility Servers. 
- In normal enterprise environments, DCs don't always respond to LLMNR requests and should be investigated if sighted.
- Increase alert severity only when LLMNR activity is correlated with NTLM authentication attempts.

---